using UnityEngine;

namespace TTT.UI
{
    public class UIInputManager : MonoBehaviour
    {
        [Tooltip("The UI context that the input is for")]
        [SerializeField]
        private UIContext context;

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
